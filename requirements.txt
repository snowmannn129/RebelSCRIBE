# RebelSCRIBE Dependencies

# UI Framework
PyQt6>=6.5.0
PyQt6-QScintilla>=2.14.0  # For advanced text editing

# AI Integration
anthropic>=0.5.0  # For Claude API
openai>=1.0.0     # For OpenAI API
httpx>=0.24.1     # For async HTTP requests
backoff>=2.2.1    # For retry logic
google-generativeai>=0.3.0  # For Google AI API

# Local AI Models (Optional)
torch>=2.0.0      # For local AI models and GPU acceleration
transformers>=4.30.0  # For Hugging Face models
datasets>=2.12.0  # For fine-tuning datasets
onnxruntime>=1.15.0  # For optimized inference (optional)
optimum>=1.12.0   # For model optimization (optional)
bitsandbytes>=0.41.0  # For model quantization (optional)
accelerate>=0.23.0  # For large model loading and multi-GPU support (optional)
peft>=0.5.0       # For parameter-efficient fine-tuning (optional)
autoawq>=0.1.7    # For AWQ quantization support (optional)
llama-cpp-python>=0.2.0  # For GGUF model support (optional)
huggingface_hub>=0.19.0  # For model registry and catalog (optional)
# Note: For GPU acceleration, ensure you have the appropriate CUDA (NVIDIA), 
# ROCm (AMD), or MPS (Apple Silicon) libraries installed for your hardware

# Cloud Storage
dropbox>=11.36.0
google-auth>=2.22.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.100.0

# Document Processing
python-docx>=0.8.11  # For Word document export
reportlab>=4.0.4     # For PDF export
markdown>=3.4.3      # For Markdown processing
beautifulsoup4>=4.12.2  # For HTML processing
pylatex>=1.4.1       # For LaTeX export
odfpy>=1.4.1         # For ODT export
ebooklib>=0.18.0     # For EPUB, MOBI, and AZW3 export

# Data Management
pyyaml>=6.0.0  # For configuration files

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1
pytest-qt>=4.2.0  # For UI testing

# Utilities
rich>=13.4.2  # For better console output
tqdm>=4.66.1  # For progress bars
pillow>=10.0.0  # For image processing
psutil>=5.9.5  # For system monitoring and memory usage tracking

# Data Visualization (for benchmark visualization)
matplotlib>=3.7.0  # For plotting
seaborn>=0.12.0   # For enhanced visualizations
pandas>=2.0.0     # For data manipulation
plotly>=5.14.0    # For interactive visualizations
nltk>=3.8.1       # For BLEU score calculation and NLP metrics
